"""
Setup file for the velo_toy package.

This file is kept for backward compatibility with older pip versions.
The primary configuration is now in pyproject.toml.

Install with: pip install -e .
"""

from setuptools import setup

# Configuration is in pyproject.toml
# This file allows installation with older pip versions
if __name__ == "__main__":
    setup()

